services:
  app:
    build:
      target: builder
    volumes:
      - ./app:/app
      - /app/node_modules
    command: sh -c "npm install && npm run dev -- --host"
    ports:
      - "5173:5173"
    environment:
      - PUBLIC_POCKETBASE_URL=http://localhost:8090
